import { WgerExerciseResultDto } from './wger-exercise-result-dto';

describe('WgerExerciseResultDto', () => {
  it('should be defined', () => {
    expect(new WgerExerciseResultDto()).toBeDefined();
  });
});
